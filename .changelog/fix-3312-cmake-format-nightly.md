---
section: Fixed
---

- Fixed the nightly formatter smoke environment so pipx-installed cmakelang can
  load representative YAML configuration, and retain a bounded formatter
  traceback tail for actionable failures (refs #3312).
