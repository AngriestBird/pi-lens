---
section: Added
---

- **CUE code formatting support** — `.cue` files are now formatted via `cue fmt -w` through the standard formatter pipeline, with smart-default policy (no config required, `cue` binary on PATH).
